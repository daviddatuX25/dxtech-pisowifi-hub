# build-announcement-room

Build a static announcement room with user onboarding, configurable promotions, issue reporting, optional browser notifications, and an authenticated admin review dashboard.
